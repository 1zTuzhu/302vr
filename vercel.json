{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ],
  "scripts": {
    "build": "chmod +x ./node_modules/.bin/vite && npm run build"
  },
  "env": {
    "NODE_ENV": "production"
  },
  "engines": {
    "node": "14.x"
  }
}
